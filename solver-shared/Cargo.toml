[package]
name = "solver-shared"
version = "0.1.0"
edition = "2021"
description = "Shared types reused across the solver-cli, rebalancer, and oracle-operator crates"
license = "MIT"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
toml = "0.8"
